[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmd = "npm install"

[phases.build]
cmd = "npm run build"

[start]
cmd = "npm start"

[build]
# Use the dist folder as the working directory for the start command
# (but since we use "npm start", it's already handled)